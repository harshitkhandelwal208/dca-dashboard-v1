{
  "name": "dca-bot-suite",
  "private": true,
  "version": "1.0.0",
  "description": "Repository wrapper for the separate DCA bot and dashboard apps",
  "scripts": {
    "postinstall": "npm install --prefix bot && npm install --prefix dashboard",
    "build": "npm run build --prefix dashboard",
    "start": "node start.js",
    "start:bot": "node start.js",
    "start:dashboard": "npm --prefix dashboard start",
    "deploy:commands": "npm --prefix bot run deploy:commands",
    "test:spreadsheets": "npm --prefix bot run test:spreadsheets"
  }
}
